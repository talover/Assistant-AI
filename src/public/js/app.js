document.addEventListener("DOMContentLoaded", function () {
    // -- hero typing --
    var typed = new Typed('#hero-text-typing', {
        strings: ['контента'],
        typeSpeed: 50,
    });
});
